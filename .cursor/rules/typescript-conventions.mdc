---
description: TypeScript — types, imports, enums, services
globs: '**/*.{ts,tsx,d.ts}'
alwaysApply: false
---

# TypeScript Conventions

## Imports

```typescript
// ✅ Barrel gốc
import { ScheduleType } from '@/enums';
import { NSchedule, CustomCardPadding, Abstract } from '@/interfaces';
import { useTableContainer } from '@/hooks';

// ❌ Import sâu file con (khi đã có barrel)
import { CustomCardPadding } from '@/interfaces/custom-component';
import { NSchedule } from '@/interfaces/schedule';

// ❌ Relative ra ngoài folder (ESLint error)
import { Option } from '../../../interfaces';
```

Alias có sẵn: `@/`, `@components/`, `@hooks/`, `@interfaces/`, `@enums/`, `@services/`, `@libs/`, `@stores/`.

## Barrel `index.ts`

Mọi folder có `index.ts` barrel → **chỉ dùng `export * from`**:

```typescript
// ✅
export * from './schedule';
export * from './custom-components.constant';

// ❌
export { ScheduleType } from './schedule.enum';
export { CustomCard } from './custom-card';
```

- File nguồn export named (`export const`, `export type`, `export enum`, …).
- Thêm module mới: một dòng `export * from './<tên-file>'` (không extension).
- Ngoại lệ: cần **rename** hoặc export tập con → mới dùng `export { X as Y } from '…'`.

## Types & interfaces

- Domain types: file `src/interfaces/<domain>.d.ts` với `export declare namespace NDomain { … }`.
- Entity base: extend `Abstract` từ `@/interfaces`.
- Domain constants: `enum` trong `src/enums/`, export qua `src/enums/index.ts`.
- Props component: `type XxxProps = { … }` hoặc `interface IXxxProps` (theo pattern file hiện có).
- Tránh `any` khi có thể; dùng type cụ thể hoặc `unknown` + narrow.

## Services

- Class extends `BaseApi` trong `src/services/`.
- Request/response typing qua namespace trong `@/interfaces` (ví dụ `NBaseApi`).

## Utilities

- Kiểm tra list rỗng: `!list?.length` (trừ khi cần so sánh số chính xác).
- Date: `dayjs` / helpers trong `@/libs`.
- Format code: Prettier (singleQuote, tabWidth 4, printWidth 100, trailingComma all).
