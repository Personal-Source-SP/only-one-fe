export enum ViewMode {
    ALL = 'all',
    DATE = 'date',
    FOLDER = 'folder',
}

export enum SortOrder {
    NEWEST = 'newest',
    OLDEST = 'oldest',
}

export enum PhotoItemsPerPage {
    TEN = 10,
    TWENTY = 20,
    FIFTY = 50,
    HUNDRED = 100,
}
