export enum SimulationContextStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}

export enum SimulationItemStatus {
    PENDING = 'pending',
    PROCESSING = 'processing',
    PAUSED = 'paused',
    STOPPED = 'stopped',
}
