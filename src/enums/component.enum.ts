export enum ElementType {
    CARD = 'card',
    TITLE = 'title',
    CONTAINER = 'container',
}

export enum CustomFilterType {
    SEARCH = 'search',
    SELECT = 'select',
}

export enum StatCardTrend {
    UP = 'up',
    DOWN = 'down',
    NEUTRAL = 'neutral',
}
