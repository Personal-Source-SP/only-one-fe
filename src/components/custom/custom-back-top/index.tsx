'use client';

import { FloatButton, FloatButtonProps } from 'antd';

export type CustomBackTopProps = FloatButtonProps;

export const CustomBackTop = (props: CustomBackTopProps) => <FloatButton {...props} />;
